(jbuild_version 1)

(library
 ((name     danbooru_lib)
  (synopsis "Bindings for Danbooru API")
  (preprocess (pps (ppx_jane -annotated-ignores ppx_driver.runner)))
  (libraries (core
              async
              cohttp-async
              yojson))))
